    <br><br>
        <a class="go_home_link" href="/user/landingPage">Go to Home Page</a><br>
<div class="  gyms gym-detail ">
    {{#each locations}}
    <h2 class="gym-name">
        {{  this.BranchName  }}
    </h2>

    <div class=" form-cont">
        <p>
            Website: 
            <a href="">
                {{  this.website    }}
            </a>
        </p>
        <p>
            Address: 
            
            {{#if this.address.streetName.length}}
                {{  this.address.streetName  }}
            {{else}}
                "N/A"
            {{/if}}
            {{#if this.address.city.length}}
                {{  this.address.city  }}
            {{else}}
                "N/A"
            {{/if}}
            {{#if this.address.state.length}}
                {{  this.address.state  }}
            {{else}}
                "N/A"
            {{/if}}
            {{#if this.address.zip.length}}
                {{  this.address.zip  }}
            {{else}}
                "N/A"
            {{/if}}
        </p>
        <p>
            Phone: 
            {{  this.phoneNumber    }}
        </p>
        <p>
            Email: {{   this.email }}
        </p>
        <p>
            Max Capacity : {{ this.maxCapacity    }}
        </p>
    </div>
        {{/each}}

</div>