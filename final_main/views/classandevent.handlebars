<br>
<br>
<br>
<a class="go_home_link" href="/user/protectedUserHomePage">Home Page</a>
<br>
<a href="/myAppointments/add">Register for classes</a>
<br>
<a id="reviews" href="/myReviews">My Reviews</a></li>
<br>
<a href="/myAppointments">Go to My Appointment</a>
<br>
<div class = "classes">
<h2>Class and event</h2>
<div class="form-cont">
    <p></p>

    {{#if classes}}
    <ul>
        {{#each classes}}
        <li>
            <h2 >{{this.className}}</h2>
            <input type="hidden" class="class_id" value="{{this._id}}">
            <p>
                Date : {{#if this.slots.Date}}
                {{
                this.slots.Date
                }}
            </p>
            {{else}}
            <p>No Dates slots available.</p>
            {{/if}}
            <p>
                Time: {{#if this.slots.timing}}
                {{
                this.slots.timing
                }}
            </p>
            {{else}}
            <p>No time slots available.</p>
            {{/if}}


            Reviews: {{#if this.reviewsText}}
            <ul>
                {{#each this.reviewsText}}
                <li>{{this}}</li>
                {{/each}}
            </ul>
            {{else}}
            <p>No reviews.</p>
                        <button class="add_class_review" data-class-id="{{this._id}}">add reviews</button>
            {{/if}}
            
        </li>
        <if condition="$error">
        <div id="error-message" class="alert alert-danger" role="alert" style="display: none;"></div>
        {{/each}}
    </ul>

    {{else}}
    <p>No classes available.</p>
    {{/if}}


</div>
</div>
<script src="/public/js/addClassReview.js"></script>