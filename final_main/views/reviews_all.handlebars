<br>
<br>
<br>
<a class="go_home_link" href="/protectedUserHomePage">Back to User Home Page</a><br>
<br>
<a href="/myReviews/add">Add Review</a>
{{#if reviews}}
  {{#each reviews}}
    <div class="myReviews">
      {{#if this.branchName}}
        <p>Branch Name: {{this.branchName}}</p>
      {{/if}}
      {{#if this.className}}
        <p>Class Name: {{this.className}}</p>
      {{else}}
        <p>Rating: {{this.rating}}</p>
      {{/if}}
      <p>{{this.reviewText}}</p>
      <a href="/myReviews/update/{{this._id}}">Edit</a>
      {{!-- <form action="/myReviews/delete/{{this._id}}" method="POST">
        <button type="submit">Delete</button>
      </form> --}}
    </div>
  {{/each}}
{{else}}
  <p>No reviews found</p>
{{/if}}